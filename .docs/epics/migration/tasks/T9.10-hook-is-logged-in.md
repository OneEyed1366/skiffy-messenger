# T9.10: useIsLoggedIn Hook

## Metadata

| Field        | Value            |
| ------------ | ---------------- |
| **ID**       | T9.10            |
| **Layer**    | L9 - State Hooks |
| **Status**   | pending          |
| **Priority** | high             |
| **Estimate** | 0.5h             |
| **Parallel** | true             |
| **Assignee** | -                |
| **Created**  | 2026-01-06       |
| **Updated**  | 2026-01-06       |

## Dependencies

| Task ID | Name      |
| ------- | --------- |
| T7.01   | authStore |

## Description

Simple hook to check if user is currently logged in. Used for conditional rendering and route guards.

## Migration Target

- **Target File**: `apps/v2/src/hooks/useIsLoggedIn/useIsLoggedIn.ts`

## Implementation

```typescript
// apps/v2/src/hooks/useIsLoggedIn/useIsLoggedIn.ts

import { useAuthStore } from "@/stores";

//#region Hook

export function useIsLoggedIn(): boolean {
  return useAuthStore((state) => state.isLoggedIn);
}

//#endregion
```

## Acceptance Criteria

- [ ] Returns true when user is logged in
- [ ] Returns false when user is not logged in
- [ ] Re-renders component when login state changes
- [ ] TypeScript strict mode passes
